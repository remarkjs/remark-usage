// Require `pi`:
var pi = require('./index.js');

// Require `fs`:
var fs = require('fs');

// Require another index file:
var other = require('../normal/index.js');

// Logs:
console.log('text', pi);
